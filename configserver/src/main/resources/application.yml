spring:
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
  application:
    name: "configserver"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
        #   search-locations: "C:/Users/Firas/IdeaProjects/firas-spring-cloud/config-repo"
           search-locations: "classpath:/config"
server:
  port: 9999
